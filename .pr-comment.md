## 🤖 Automated review (taskferry)

_Effort: medium · Model(s): minimax/MiniMax-M3 (finders), openai/gpt-5.6-luna (verifiers, max) · 2026-08-06T13:26+ UTC · Verified findings: 6_

### Raw candidate list (pre-verification, 8 angles, 30 findings merged to 8 candidates)

**Correctness findings (6 candidates):**
1. **lib/utils/static.js:32** — readFileDirect error suppression masks real failures (EACCES, EISDIR, EMFILE)
2. **lib/openrouter.js:269** — fuzzy fallback cache index mismatch (last-written record / first-iterated split) [REFUTED by verifier]
3. **lib/openrouter.js:263** — normalizeAlias vs normalizeKey mismatch in candidate loop
4. **lib/utils/static.js:53** — dev mode fallback serves stale bytes for non-JS/CSS/HTML files
5. **tests/unit/lib/openrouter.test.js:35** — test mutates module-global cache without restore
6. **tests/unit/lib/openrouter.test.js:53** — test verification gap (only checks non-null, not values)

**Efficiency findings (2 candidates):**
7. **lib/openrouter.js:265** — buildModelAliases rebuilt on every fuzzy fallback miss (O(N×A) when O(1) possible)
8. **lib/openrouter.js:264** — fuzzy fallback loop is largely vestigial [REFUTED by verifier]

---

### Verified findings (6 CONFIRMED, 0 PLAUSIBLE, 2 REFUTED)

Survivors ranked most-severe first:

**Correctness (High):**

1. **lib/utils/static.js:32** — Error suppression masks misconfiguration
   - Scenario: Directory substituted for .html file, or permission lock → fs throws EISDIR/EACCES → caught and returns null → 404 instead of 403/500
   - Impact: Ops has no signal of misconfiguration in production
   - Verdict: **CONFIRMED**

2. **lib/openrouter.js:263** — normalizeAlias/normalizeKey mismatch creates candidate-loop gaps
   - Scenario: Catalog id='openai_gpt-4' indexed as 'openai_gpt-4'; caller passes 'openai-gpt-4' → candidates miss, fuzzy matches via normalization
   - Impact: Two code paths match same input asymmetrically; could silently fall through to default pricing
   - Verdict: **CONFIRMED**

3. **lib/utils/static.js:53** — Dev stale-byte serve on non-JS/CSS/HTML files
   - Scenario: isDev=true, request /dashboard/data.json → readFileCached returns in-memory cached bytes; developer edits file on disk; browser refresh still shows pre-edit version until restart
   - Impact: Development friction; misleading debugging experience
   - Verdict: **CONFIRMED**

**Test Correctness (Medium):**

4. **tests/unit/lib/openrouter.test.js:35** — Test cache pollution
   - Scenario: setOpenRouterPricingSnapshot mutates module-global cache, never restored; later test in same Bun process reads stale snapshot
   - Impact: Order-dependent test failures; brittle suite
   - Verdict: **CONFIRMED**

5. **tests/unit/lib/openrouter.test.js:53** — Incomplete test assertion
   - Scenario: Test only asserts result !== null, ignores record identity and pricing.prompt/completion values
   - Impact: Future regressions (wrong record returned) pass the test silently
   - Verdict: **CONFIRMED**

**Efficiency (Medium):**

6. **lib/openrouter.js:265** — buildModelAliases rebuilt per fuzzy miss
   - Scenario: Each cache.index miss triggers O(N) iteration over cache.models, calling buildModelAliases(record) anew
   - Impact: O(N×A) cost per lookup; could be O(1) with precomputed normalized-alias index
   - Verdict: **CONFIRMED**

---

**Refuted (not in final report):**
- lib/openrouter.js:269 — Cache index overwrite (verifier: candidates loop catches it first)
- lib/openrouter.js:264 — Vestigial fallback (verifier: handles separator-normalization cases not in index)
