# SDD ledger — plan: .superpowers/plans/2026-07-28-dataviz-league-table.md

Task 1: dispatched implementer oc_mshu8mh0_6938bee7 (opencode-go/minimax-m3, --executor opencode) at BASE fb5956f
Task 1: implementer DONE_WITH_CONCERNS (justified fixture fix, verified), accepted, committed dc2728e
Task 1: dispatched task reviewer oc_mshukneg_becc326a (openai/gpt-5.6-luna, max effort)
Task 1: review verdict = Needs fixes. 2 Important findings, both on plan-verbatim code (C7/C19 comments), escalated to human:
  - Finding 1 (dashboard/js/league-table.js:50-57, effective-rate source uses pricingByModel not costsByModel): RULING (human) — keep pricingByModel/calculateCostWithPricing as-is; plan's own Step 3 code + C7 rationale governs over imprecise Global Constraints prose. NOT a defect. No fix needed.
  - Finding 2 (dashboard/js/league-table.js:41-47, memoization only keys on weeklyData, not pricingByModel -> stale badges on pricing-only change): RULING (human) — fix now. Extend the cache-invalidation guard to also key on pricingByModel reference.
  - Minor "Extra" finding (config/eslint-baseline.json unrelated cleanup) — deferred to final whole-branch review triage, not blocking.
Task 1: NEXT ACTION — dispatch fix round 1/5, RESUME implementer session ses_027b4d450ffe1M5NewOQ6rrdm6 (opencode-go/minimax-m3, --executor opencode, --session-id ses_027b4d450ffe1M5NewOQ6rrdm6) with Finding 2 only (Finding 1 explicitly not a defect per ruling above — tell the implementer this explicitly so it doesn't "fix" it). After fix lands: bun test tests/unit/league-table.test.js for real, commit, sdd-review-package over the fix range, scoped re-review on openai/gpt-5.6-luna --executor opencode --variant max.
Task 1: fix round 1/5 (1 addressed, 0 open — Finding 2 badge-cache staleness on pricing change fixed via _lastPricingRef guard; Finding 1 confirmed untouched per ruling; commits dc2728e..35c82cf). Real verification: bun test tests/unit/league-table.test.js 7/7 pass, bun run test 594/594 pass. NEXT ACTION: sdd-review-package over dc2728e..35c82cf, dispatch scoped re-review on openai/gpt-5.6-luna --executor opencode --variant max.
Task 1: re-review oc_mshvie4d_c881343a (openai/gpt-5.6-luna, max) — Finding 2 ADDRESSED, no new breakage, Approved. (Note: reviewer cited line numbers 839-847/874-905 that don't exist in this ~65-line file — hallucinated specifics; substantive verdict independently confirmed via direct diff read + real `bun test`/`bun run test` runs, so treated as reliable despite the citation error.)
Task 1: complete (commits fb5956f..35c82cf, review clean after 1 fix round)
Task 2: dispatched task reviewer oc_mshw3szm_9b0532fd (openai/gpt-5.6-luna, max) at BASE cdce888
Task 2: review verdict = Needs fixes. 2 Important findings, both confirmed real by orchestrator:
  - Finding 1 (compare.js:47-69, expansion state resets on every ambient SSE re-render): confirmed via analytics.js's own "ambient refreshes rebuild the same markup" comment. Real bug.
  - Finding 2 (design-v2.css .models-table-container overflow:hidden overrides main.css's overflow-x:auto for the same class, risking clipping at 360px): plan's own Global Constraints flagged this exact risk and required verification during Task 2 Step 10 — implementer's report never mentions checking it. Not plan-mandated-as-OK; a missed verification step. Real, actionable.
  - Minor: eslint-baseline suppressions instead of resolving warnings; toggle test coverage gaps (collapse, keyboard). Deferred to final review triage.
Task 2: NEXT ACTION dispatched — fix round 1/5, RESUME implementer session ses_027924736ffeytqwYYmcPUYLfQ (task oc_mshwgc7u_8f2e0057) with both Important findings.
Task 2: fix round 1/5 (2 addressed, 0 open — Finding 1 expansion-state preserved via wasExpanded read-before-rebuild; Finding 2 fixed via scoped #compare-chart-container{overflow-x:auto} override, brief's claim that Models tab already handled this was wrong, both tabs shared the same overflow:hidden bug; commits cdce888..a2d0e71). Real verification: bun run test 601/601, tsc clean, mobile.spec.js 8/8 (started dashboard server on :7071 myself — test:e2e only covers overflow.spec.js, mobile.spec.js needs a separate run), overflow.spec.js 9/10 (1 known pre-existing). Note: implementer's report characterized a mobile.spec.js "iPhone 14 Pro" failure as a deterministic pre-existing bug reproducing on 602eae0; independently re-ran it twice (pre-fix and post-fix) and it passed both times — likely flaky/time-relative (playwright-fixtures.js uses Date.now()-relative mock timestamps), not deterministic. Unrelated to this diff either way (touches unrelated .top-model-card/historyData code), so doesn't block. NEXT ACTION: sdd-review-package over cdce888..a2d0e71, dispatch scoped re-review on openai/gpt-5.6-luna --executor opencode --variant max.
Task 2: re-review oc_mshwzty6_f5ed6031 (openai/gpt-5.6-luna, max) on fix round 1 — both original findings ADDRESSED, but flagged new breakage: expanded-state re-render omits .league-other-row class from the others rows, so the toggle handler's querySelectorAll('.league-other-row') captures an empty NodeList and clicking "Hide" afterward no longer actually hides anything. Confirmed real via direct code trace (compare.js:64-66's rowHtml(row, !wasExpanded) + the post-render querySelectorAll snapshot). Needs fixes.
Task 2: NEXT ACTION dispatched — fix round 2/5, RESUME implementer session ses_027924736ffeytqwYYmcPUYLfQ (task oc_mshx3yin_31f8059b).
