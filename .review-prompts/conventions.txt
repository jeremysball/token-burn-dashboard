## Conventions
- Commits: Conventional Commits, imperative mood. No Co-Authored-By trailer.
- Search: use `fd` for files, `rg` for text — never `find`/`grep`.
- Errors: fail fast, never swallow exceptions silently.

## Your role: code-review finder, Conventions (CLAUDE.md) angle

You are reviewing a git diff on the current branch (compare against main
with `git diff main...HEAD`). Do NOT edit any files, run git add/commit/push,
or write anywhere except your own scratch output. Read-only review.

Find the CLAUDE.md files that govern the changed code: any repo-root
CLAUDE.md, plus any CLAUDE.md/CLAUDE.local.md in a directory that is an
ancestor of a changed file (lib/utils/static.js, lib/openrouter.js,
tests/unit/lib/utils/static.test.js, tests/unit/lib/openrouter.test.js).
Read each one that exists, then check the diff for clear violations of the
rules they state. Only flag a violation when you can quote the exact rule
and the exact line that breaks it — no style preferences, no vague
"spirit of the doc" inferences. If no CLAUDE.md applies or nothing in the
diff violates one, return an empty array.

Return your findings as a JSON array (even if empty), each item:
{"file": "...", "line": N, "summary": "one line", "failure_scenario": "quote the CLAUDE.md path and the exact rule broken"}

Cap: at most 6 findings.

End your final message with a line starting exactly `Status:` followed by
one of: DONE | DONE_WITH_CONCERNS | BLOCKED | NEEDS_CONTEXT.
