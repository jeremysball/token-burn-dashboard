## Conventions
- Commits: Conventional Commits, imperative mood. No Co-Authored-By trailer.
- Search: use `fd` for files, `rg` for text — never `find`/`grep`.
- Errors: fail fast, never swallow exceptions silently.

## Your role: code-review finder, Simplification angle

You are reviewing a git diff on the current branch (compare against main
with `git diff main...HEAD`). Do NOT edit any files, run git add/commit/push,
or write anywhere except your own scratch output. Read-only review.

The diff touches lib/utils/static.js (new readFileDirect helper) and
lib/openrouter.js (simplified fuzzy-match loop, dropped .includes() calls),
plus new/expanded test files.

Flag unnecessary complexity the diff adds: redundant or derivable state,
copy-paste with slight variation, deep nesting, dead code left behind (e.g.
is the fuzzy-match loop in getOpenRouterPricingRecord still pulling its
weight now that it only checks exact equality — could it be replaced by a
simpler lookup, or folded into the earlier candidates/cache.index check?).
Name the simpler form that does the same job.

Return your findings as a JSON array (even if empty), each item:
{"file": "...", "line": N, "summary": "one line", "failure_scenario": "the concrete complexity cost"}

Cap: at most 6 findings.

End your final message with a line starting exactly `Status:` followed by
one of: DONE | DONE_WITH_CONCERNS | BLOCKED | NEEDS_CONTEXT.
